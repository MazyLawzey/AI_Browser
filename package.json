{
  "name": "ai-helper",
  "version": "1.0.0",
  "description": "AI Helper for Electron",
  "main": "dist/main.js",
  "scripts": {
    "copy-assets": "shx cp src/electron/tooltip.html dist/electron/tooltip.html && shx cp src/electron/tooltip.js dist/electron/tooltip.js && shx cp src/electron/agent.html dist/electron/agent.html",
    "build": "tsc && npm run copy-assets",
    "start": "npm run build && electron dist/main.js",
    "agent": "npm run build && electron dist/agent.js",
    "dev": "tsc --watch",
    "scratch": "shx rm -rf dist && npm run start"
  },
  "author": "MazyLawzey",
  "license": "MIT",
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/node": "^20.19.40",
    "electron": "^42.0.1",
    "shx": "^0.4.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ollama": "^0.6.3"
  }
}
